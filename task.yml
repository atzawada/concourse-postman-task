---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: atzawada/concourse-postman-task

inputs:
  - name: git-repo

outputs:
  - name: results

run:
  path: /bin/script